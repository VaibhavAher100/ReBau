# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## HackMate

<one-line purpose - TODO>

## Commands

- Setup: `python -m venv .venv && .venv\Scripts\activate && pip install -e .`
- Test: `pytest`
- Single test: `pytest path/to/test.py::test_name`
- Lint: `ruff check .` (TODO: confirm tooling)

## Conventions

Global conventions (`~/.claude/CLAUDE.md`) apply.
